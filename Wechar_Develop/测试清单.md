# 微信公众号前两个功能测试清单

使用说明：每一项完成后打勾；如果失败，在“备注”里写错误现象和时间。

---

## A. 环境与启动检查

- [ ] A1. 腾讯地图 Key 已填写到地点查询页面（功能一）。
- [ ] A2. 后端依赖安装完成（Flask、requests、waitress 等）。
- [ ] A3. `.env` 已配置，`WECHAT_TOKEN` 已填写。
- [ ] A4. 后端服务启动成功（本地或服务器端无报错）。
- [ ] A5. Nginx 启动成功，配置校验通过。

备注：

---

## B. 页面可达性检查（本地）

- [ ] B1. 打开统一入口页面：`http://localhost:5000/`
- [ ] B2. 打开智能问答页面：`http://localhost:5000/chat`
- [ ] B3. 打开地点查询页面：`http://localhost:5000/place`
- [ ] B4. 三个页面移动端宽度下显示正常（无明显错位/遮挡）。

备注：

---

## C. 功能一：地点查询测试

### C1. 正常路径
- [ ] C1-1. 输入“北京理工大学”后可返回结果。
- [ ] C1-2. 地图中心移动到目标位置。
- [ ] C1-3. 地图上出现地点标记。
- [ ] C1-4. 右侧地点名称与地址更新。
- [ ] C1-5. 静态地图图片正常加载。

### C2. 异常路径
- [ ] C2-1. 输入空字符串，页面提示“请先输入关键词”类信息。
- [ ] C2-2. 输入无效关键词（如随机乱码），页面提示“未找到匹配地点”。
- [ ] C2-3. Key 错误时，页面有明确失败提示，不会白屏。

备注：

---

## D. 功能二：智能问答测试（网页）

### D1. 正常路径
- [ ] D1-1. 发送“新生什么时候选课”能返回答案。
- [ ] D1-2. 发送“校园网怎么开通”能返回答案。
- [ ] D1-3. 连续发送 3 个问题，页面交互正常（不丢消息、不重复）。
- [ ] D1-4. 回答内容可读，语气符合“新生助手”。

### D2. FAQ 命中验证
- [ ] D2-1. 修改 FAQ 文件后重启服务，回答随内容变化。
- [ ] D2-2. FAQ 未命中时，能返回“建议查看官网/联系辅导员”类兜底答案。

### D3. 异常路径
- [ ] D3-1. 网络断开时前端显示“网络异常”类提示。
- [ ] D3-2. 提交空问题时接口返回 400，前端不崩溃。

备注：

---

## E. 接口级测试

- [ ] E1. `POST /api/chat` 传入合法 JSON 返回 200 和 `answer` 字段。
- [ ] E2. `POST /api/chat` 传入空问题返回 400。
- [ ] E3. `GET /healthz` 返回 200（若已启用）。

备注：

---

## F. 公众号接入测试（公网）

### F1. 基础连通
- [ ] F1-1. 公网可访问 `https://你的域名/`。
- [ ] F1-2. 公网可访问 `https://你的域名/chat`。
- [ ] F1-3. 公网可访问 `https://你的域名/place`。
- [ ] F1-4. 公网可访问 `https://你的域名/wechat`（由微信校验流程验证）。

### F2. 后台配置
- [ ] F2-1. 微信后台 Token 与 `.env` 中一致。
- [ ] F2-2. 服务器 URL 已配置为 `https://你的域名/wechat`。
- [ ] F2-3. 菜单已配置统一入口或分菜单。

### F3. 真机验证
- [ ] F3-1. 在公众号点击菜单可进入页面。
- [ ] F3-2. 发送文本消息可收到智能回复。
- [ ] F3-3. 连续提问 3 次无超时或异常报错。

备注：

---

## G. 稳定性与运行维护

- [ ] G1. Waitress 以服务方式运行（NSSM）。
- [ ] G2. 服务重启后可自动恢复。
- [ ] G3. Nginx 重载后业务不受影响。
- [ ] G4. 日志中无持续报错（连续观察 15 分钟）。

备注：

---

## H. 验收结论

- [ ] 全部通过，可上线。
- [ ] 部分通过，需修复后复测。

未通过项汇总：

修复负责人：

复测时间：
